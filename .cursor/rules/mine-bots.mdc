---
alwaysApply: true
---
## 🔐 Безопасность

- **Никогда не храни секреты в коде**:
  - Используй `.env`, переменные окружения.
  - Не коммить `.env`, `credentials.json` и т.п.
- Пароли хешируй с помощью **bcrypt** или **argon2**.
- Используй только **HTTPS** для всех внешних API.
- Аутентификация — через **JWT** с ограниченным сроком действия.
- Реализуй **RBAC** на каждом защищённом эндпоинте.
- **Не логируй персональные данные пользователей**.
- Все входные данные: **валидация + санитация**.
- Проверяй зависимости на уязвимости, регулярно обновляй.
- Межсервисное взаимодействие — только через **защищённые каналы** (mTLS, VPN).
- Для критичных операций — **2FA или подтверждение через email/SMS**.
- Избегай CORS-уязвимостей — явно указывай разрешённые источники.

## 🛠️ Процесс разработки

- Все изменения фиксируй в `docs/changelog.md` и `docs/tasktracker.md`.
- Перед каждым шагом — **подтверждение у владельца проекта**.
- После — краткое резюме (до 5 пунктов).
- Предлагай **альтернативы** при спорных решениях.
- Регулярно **напоминай о статусе задачи**.
- Обязательно проводи **code review** всех изменений.
- Удаляй неиспользуемый код и комментарии.

## 📁 Документирование

### Структура и код

- В начале каждого нового файла добавляй:

  ```python
  """
  @file: [имя файла]
  @description: [краткое описание]
  @dependencies: [связанные компоненты/файлы]
  @created: [дата]
  """
  ```

- После каждого изменения — актуализируй:
  - `docs/project.md` (архитектура, компоненты, схемы)
  - API-документацию (например, в Swagger/OpenAPI)

### Changelog (`changelog.md`)

```
## [2025-09-13] - Пример изменений

### Добавлено
- Поддержка виртуального окружения
- Скрипты автоматической настройки

### Изменено
- Обновлены зависимости aiogram

### Исправлено
- Ошибка в конфигурации ADMIN_IDS
```

### Task tracker (`tasktracker.md`)

```
## Задача: Реализация обработчиков постов
- **Статус**: В процессе
- **Описание**: Создание handlers для команд и сообщений
- **Шаги выполнения**:
  - [x] Создана структура FSM состояний
  - [x] Реализованы сервисы постов и тегов
  - [ ] Создание обработчиков команд
- **Зависимости**: База данных PostgreSQL
```

## 📣 Коммуникация

- Если требования не ясны — **задавай конкретные вопросы**.
- При выборе между вариантами — **объясняй плюсы и минусы**.
- Для крупных задач — **предлагай декомпозицию**.
- В конце каждой сессии — **отчёт + план**.

## ✅ Коммиты

- Все коммиты должны соответствовать **Conventional Commits**:

| Тип         | Назначение                                        |
|-------------|----------------------------------------------------|
| `feat:`     | Добавление новой функциональности                  |
| `fix:`      | Исправление ошибок                                 |
| `docs:`     | Изменения в документации                           |
| `refactor:` | Переписывание кода без изменения поведения         |
| `test:`     | Добавление/изменение тестов                        |
| `chore:`    | Обслуживание проекта                               |

## 🤖 Хороший тон для Telegram-ботов

- Соблюдай **минимализм**: не перегружай пользователя лишними сообщениями или кнопками.
- Используй **inline-кнопки** вместо лишних текстовых команд, если это повышает UX.
- Все **сообщения бота** должны быть краткими, информативными и человеко-ориентированными.
- **Обрабатывай ошибки** gracefully — сообщай пользователю, что пошло не так, без технических подробностей.
- Используй **FSM (Finite State Machine)** для сложных сценариев взаимодействия.
- Все команды и состояния должны быть **логически разделены** по модулям.
- Используй **middleware** для логирования, ограничения доступа и других сквозных задач.
- Не злоупотребляй **редактированием сообщений** — это может путать пользователя.
- Поддерживай **межплатформенность**: работоспособность как в мобильной, так и в десктопной версиях Telegram.
- Обязательно реализуй **логирование действий** пользователя (без PII) для отладки.
- Все ID пользователей и чатов — **типизированные** (`int | str`), валидация обязательна.
- Реализуй команды `/start`, `/help` и fallback для неизвестных запросов.
- Учитывай **временные зоны пользователей** при отправке напоминаний и планировании событий.
- Разграничивай **права доступа** (например, админ / пользователь / модератор).
- При отправке медиа (фото, видео, документы) — **валидируй размер и формат** заранее.

## 🐍 Специфика Python/aiogram

- Используй **async/await** для всех асинхронных операций.
- Применяй **типизацию** с помощью `typing` и `typing_extensions`.
- Используй **pydantic** для валидации данных.
- Реализуй **dependency injection** через middleware.
- Применяй **контекстные менеджеры** для работы с ресурсами.
- Используй **logging** вместо print для отладки.
- Применяй **dataclasses** для простых структур данных.
- Используй **enums** для констант и состояний.
- Применяй **f-strings** для форматирования строк.
- Используй **pathlib** для работы с путями.

## 🗄️ Работа с базой данных

- Используй **asyncpg** для асинхронной работы с PostgreSQL.
- Применяй **пулы подключений** для оптимизации производительности.
- Используй **транзакции** для атомарных операций.
- Применяй **prepared statements** для безопасности.
- Используй **миграции** для изменения схемы БД.
- Применяй **индексы** для оптимизации запросов.
- Используй **soft delete** вместо физического удаления.
- Применяй **денормализацию** для кеширования часто используемых данных.

## 🚀 Развертывание и DevOps

- Используй **виртуальные окружения** для изоляции зависимостей.
- Применяй **Docker** для контейнеризации.
- Используй **systemd** для управления сервисами.
- Применяй **логирование** с ротацией файлов.
- Используй **мониторинг** и **алерты**.
- Применяй **backup** для критических данных.
- Используй **CI/CD** для автоматизации развертывания.

## 💡 Главное правило

> **Перед началом изменений — актуализируй документацию. Только потом приступай к разработке.**