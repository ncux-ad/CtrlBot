#!/bin/bash
# Скрипт для быстрого перезапуска в режиме разработки

echo "🚀 Быстрый перезапуск CtrlBot (режим разработки)"

# Останавливаем контейнер
echo "⏹️ Останавливаем контейнер..."
docker-compose stop ctrlbot

# Запускаем контейнер (код монтируется через volume)
echo "▶️ Запускаем контейнер..."
docker-compose up -d ctrlbot

# Ждем запуска
echo "⏳ Ждем запуска..."
sleep 5

# Проверяем статус
echo "📊 Статус контейнера:"
docker-compose ps ctrlbot

echo "✅ Готово! Изменения в коде применены автоматически."
echo "📝 Логи: docker logs ctrlbot -f"
