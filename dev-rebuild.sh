#!/bin/bash
# Скрипт для полной пересборки (только когда нужно)

echo "🔨 Полная пересборка CtrlBot"

# Останавливаем все контейнеры
echo "⏹️ Останавливаем контейнеры..."
docker-compose down

# Пересобираем образ
echo "🔨 Пересобираем образ..."
docker-compose build --no-cache

# Запускаем контейнеры
echo "▶️ Запускаем контейнеры..."
docker-compose up -d

# Ждем запуска
echo "⏳ Ждем запуска..."
sleep 10

# Проверяем статус
echo "📊 Статус контейнеров:"
docker-compose ps

echo "✅ Готово! Полная пересборка завершена."
echo "📝 Логи: docker logs ctrlbot -f"
